#####################################################################
#   Spoolman & ACE Integration for Anycubic Kobra 3 / S1
#####################################################################
#
# PREREQUISITES:
# 1. Spoolman must be installed and configured in moonraker.conf.
# 2. [save_variables] MUST be defined in your printer configuration 
#    (usually provided by ace_KS1.cfg or ace_K3.cfg).
#
# HOW TO USE:
#
# A) FilaMan SKU Format (Recommended):
#    - Write "VENDOR-MATERIAL-SPOOLID" onto the tag (e.g., "ESUN-PLA-42").
#    - The last segment after '-' is parsed as FilaMan Spool ID automatically.
#    - This matches the format used by moonraker_lane_sync._extract_spool_id.
#
# B) Rewritable RFID Tags (Pure Number):
#    - Write the Spool-ID (Integer, e.g., "15") directly onto the tag.
#    - The logic will automatically load Spool ID #15.
#
# C) Original Anycubic RFID Tags (Locked) / Static SKU Mapping:
#    - Map the recognized SKU to your Spoolman/FilaMan ID via the UI.
#    - Use the macro: MAP_SKU SKU="HPL17-103" ID=1
#    - All mappings are permanently saved in your variables.cfg.
#
# D) Manual Assignment (No RFID):
#    - Use the macro: SPOOLMAN_MANUAL_SLOT SLOT=<0-3> ID=<Spool_ID>
#
#####################################################################

[gcode_macro SET_ACTIVE_SPOOL]
gcode:
  {% if params.ID and params.ID|int > 0 %}
    {action_call_remote_method("spoolman_set_active_spool", spool_id=params.ID|int)}
  {% else %}
    {action_call_remote_method("spoolman_set_active_spool", spool_id=None)}
  {% endif %}

[gcode_macro CLEAR_ACTIVE_SPOOL]
gcode:
  {action_call_remote_method("spoolman_set_active_spool", spool_id=None)}

[gcode_macro _SET_SPOOL_BY_TOOL]
gcode:
    {% set tool = params.TOOL|int %}
    {% set sv = printer.save_variables.variables %}
    
    {% set inv_key = 'ace_inventory_0' if tool < 4 else 'ace_inventory_1' %}
    {% set inv_idx = tool if tool < 4 else tool - 4 %}
    {% set hw_sku = "none" %}
    {% set hw_status = "unknown" %}
    
    {% if inv_key in sv %}
        {% set hw_sku = sv[inv_key][inv_idx].sku|default("none")|string|trim %}
        {% set hw_status = sv[inv_key][inv_idx].status|default("unknown")|string %}
    {% endif %}

    {% set m_id = sv["manual_spool_id_" ~ tool]|default(0)|int %}
    {% set m_lock_sku = sv["manual_spool_lock_sku_" ~ tool]|default("none")|string %}

    {% set rfid_id = 0 %}
    
    # --- DYNAMISCHES SKU MAPPING (NUR AUS VARIABLES.CFG) ---
    {% set sku_map = sv.sku_map|default({}) %}
    # -------------------------------------------------------
    
    # --- LOOKUP: sku_map → last-segment → pure number ---
    {% if hw_sku in sku_map %}
        {% set rfid_id = sku_map[hw_sku] %}
    {% elif '-' in hw_sku and hw_sku.split('-')[-1].isdigit() %}
        {% set rfid_id = hw_sku.split('-')[-1]|int %}
    {% elif hw_sku | int(0) > 0 %}
        {% set rfid_id = hw_sku | int %}
    {% endif %}

    {% if hw_status == "empty" %}
        CLEAR_ACTIVE_SPOOL
    {% elif m_id > 0 and hw_sku == m_lock_sku %}
        SET_ACTIVE_SPOOL ID={m_id}
        {action_respond_info("Spoolman: manually lock (ID %d) für Slot %d" % (m_id, tool))}
    {% elif rfid_id > 0 %}
        {% if m_id > 0 and hw_sku != m_lock_sku %}
            SAVE_VARIABLE VARIABLE=manual_spool_id_{tool} VALUE=0
        {% endif %}
        SET_ACTIVE_SPOOL ID={rfid_id}
        {action_respond_info("FilaMan: Spool ID %d active (Slot %d, SKU '%s')" % (rfid_id, tool, hw_sku))}
    {% else %}
        CLEAR_ACTIVE_SPOOL
        {action_respond_info("Spoolman: No ID found for SKU '%s' gefunden (Slot %d)" % (hw_sku, tool))}
    {% endif %}

[gcode_macro MAP_SKU]
description: Verknüpft eine SKU dauerhaft mit einer Spoolman ID
gcode:
    {% set sku = params.SKU|default("")|string %}
    {% set id = params.ID|default(0)|int %}
    
    {% if sku == "" or id == 0 %}
        {action_respond_info("Fehler: Bitte SKU und ID angeben.")}
    {% else %}
        {% set sv = printer.save_variables.variables %}
        {% set current_map = sv.sku_map|default({}) %}
        {% set _dummy = current_map.update({sku: id}) %}
        SAVE_VARIABLE VARIABLE=sku_map VALUE="{current_map}"
        {action_respond_info("Spoolman: SKU '%s' wurde dauerhaft mit ID %d verknuepft." % (sku, id))}
    {% endif %}

[gcode_macro UNMAP_SKU]
description: Loescht eine verknuepfte SKU aus dem Speicher
gcode:
    {% set sku = params.SKU|default("")|string %}
    {% set sv = printer.save_variables.variables %}
    {% set current_map = sv.sku_map|default({}) %}
    
    {% if sku in current_map %}
        {% set _dummy = current_map.pop(sku) %}
        SAVE_VARIABLE VARIABLE=sku_map VALUE="{current_map}"
        {action_respond_info("Spoolman: SKU '%s' wurde aus dem Speicher geloescht." % sku)}
    {% else %}
        {action_respond_info("Spoolman: SKU '%s' wurde nicht gefunden." % sku)}
    {% endif %}

[gcode_macro SPOOLMAN_MANUAL_SLOT]
description: Links a manual ID to the current spool in the slot
gcode:
    {% set slot = params.SLOT|default(-1)|int %}
    {% set id = params.ID|default(0)|int %}
    
    {% if slot >= 0 and slot <= 7 %}
        {% set sv = printer.save_variables.variables %}
        
        # 1. Read current hardware SKU from ACE inventory
        {% set inv_key = 'ace_inventory_0' if slot < 4 else 'ace_inventory_1' %}
        {% set inv_idx = slot if slot < 4 else slot - 4 %}
        {% set current_hw_sku = "none" %}
        
        {% if inv_key in sv %}
            {% set current_hw_sku = sv[inv_key][inv_idx].sku|default("none")|string %}
        {% endif %}

        # 2. save the ID AND the SKU to which this ID should apply.
        SAVE_VARIABLE VARIABLE=manual_spool_id_{slot} VALUE={id}
        SAVE_VARIABLE VARIABLE=manual_spool_lock_sku_{slot} VALUE='"{current_hw_sku}"'
        
        {action_respond_info("Spoolman: Slot %d manually locked to ID %d (base SKU: %s)" % (slot, id, current_hw_sku))}
    {% else %}
        {action_respond_info("Error: SLOT must be between 0 and 7.")}
    {% endif %}

#####################################################################
#   T-macros trigger tool changes Spoolman
#   Updated to support dynamic PURGELENGTH from post-processing
#####################################################################

[gcode_macro T0]
gcode:
  {% set p_len = params.PURGELENGTH|default(0) %}
  ACE_CHANGE_TOOL TOOL=0 PURGELENGTH={p_len}
  _SET_SPOOL_BY_TOOL TOOL=0

[gcode_macro T1]
gcode:
  {% set p_len = params.PURGELENGTH|default(0) %}
  ACE_CHANGE_TOOL TOOL=1 PURGELENGTH={p_len}
  _SET_SPOOL_BY_TOOL TOOL=1

[gcode_macro T2]
gcode:
  {% set p_len = params.PURGELENGTH|default(0) %}
  ACE_CHANGE_TOOL TOOL=2 PURGELENGTH={p_len}
  _SET_SPOOL_BY_TOOL TOOL=2

[gcode_macro T3]
gcode:
  {% set p_len = params.PURGELENGTH|default(0) %}
  ACE_CHANGE_TOOL TOOL=3 PURGELENGTH={p_len}
  _SET_SPOOL_BY_TOOL TOOL=3

[gcode_macro T4]
gcode:
  {% set p_len = params.PURGELENGTH|default(0) %}
  ACE_CHANGE_TOOL TOOL=4 PURGELENGTH={p_len}
  _SET_SPOOL_BY_TOOL TOOL=4

[gcode_macro T5]
gcode:
  {% set p_len = params.PURGELENGTH|default(0) %}
  ACE_CHANGE_TOOL TOOL=5 PURGELENGTH={p_len}
  _SET_SPOOL_BY_TOOL TOOL=5

[gcode_macro T6]
gcode:
  {% set p_len = params.PURGELENGTH|default(0) %}
  ACE_CHANGE_TOOL TOOL=6 PURGELENGTH={p_len}
  _SET_SPOOL_BY_TOOL TOOL=6

[gcode_macro T7]
gcode:
  {% set p_len = params.PURGELENGTH|default(0) %}
  ACE_CHANGE_TOOL TOOL=7 PURGELENGTH={p_len}
  _SET_SPOOL_BY_TOOL TOOL=7

